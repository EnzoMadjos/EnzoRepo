Goal: {{one-line business goal}}
Current schema: {{link or attach object/field list}}
Constraints: {{performance, storage, sharing model, backward-compatibility}}
Deliverables:
- JSON schema proposal
- `objects/` metadata snippets (API names + types)
- Migration plan (steps + rollback)

Example: "Goal: Add Orders object to capture sales orders. Current schema: Account, Contact. Constraints: must link to Account, 1M records expected. Deliverables: ..."
