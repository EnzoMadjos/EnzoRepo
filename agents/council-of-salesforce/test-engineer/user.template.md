Target artifact: {{class or LWC}}
Coverage target: {{percentage}}
Data setup: {{factory rules}}
Deliverables: test files, commands to run locally (`sfdx` or `npm test`), coverage summary.

Example: "Target: OrderService.cls, Coverage: 90%, Data: factory for Order__c with related Account."
