[alembic]
script_location = alembic

[alembic:runtime]
# URL is set programmatically in alembic/env.py via config.get_database_url()
# Do not hardcode here — supports both postgres and sqlite backends
sqlalchemy.url = sqlite:///./secure/pitogo.db
