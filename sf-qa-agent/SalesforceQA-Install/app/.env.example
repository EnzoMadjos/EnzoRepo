# LLM (local Ollama)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2:3b

# Optional: change the port the app listens on
APP_PORT=8200

# NOTE: Salesforce credentials are entered at login — not stored here.
